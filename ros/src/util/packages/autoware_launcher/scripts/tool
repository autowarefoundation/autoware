#! /usr/bin/env python

import logging
import sys

from autoware_launcher.tool import editor

logging.basicConfig(level=logging.DEBUG, format="%(levelname)-5s: %(name)s: %(message)s")

sys.exit(editor.main(sys.argv))