name :
subs :
  - name : Lane Navigator
    run  : rosrun lane_navigator lane_navigator swap_x_y_on

  - name : Fake Drivers
    subs :
      - name : Canera
        run  : 
      - name : CAN
        run  : 
      - name : GNSS
        run  : 
      - name : LIDAR
        run  : 
  - name : Sample Map
    subs :
      - name : Point
        subs :
        - name : Original
          run  :
        - name : Filtered
          run  : 
      - name : Vector
        subs :
        - name : Zebra
          run  :
        - name : Lane
          run  : 
  - name : Sample Mobility
    subs :
      - name : All
        run  : rosrun sample_data sample_mobility

buttons:
  rosbag_play :
    run : rosbag play
  pmap :
    run :
      selobj : checkbox_point_map_update
      False  : rosrun sample_data sample_points_map
      True   : rosrun sample_data sample_points_map_update swap_x_y_on
  vmap :
    run : rosrun sample_data sample_vector_map __args__ swap_x_y_on
  trajectory :
    run : rosrun sample_data sample_trajectory __args__ swap_x_y_on

checkboxs:
  sim_time :
    run :
      False  : rosparam set use_sim_time false
      True   : rosparam set use_sim_time true
