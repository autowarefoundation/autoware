name :
subs :
  - name : Fake Drivers
    subs :
      - name : Canera
        run  : 
      - name : CAN
        run  : 
      - name : GNSS
        run  : 
      - name : LIDAR
        run  : 
  - name : Simple Map
    subs :
      - name : Point
        subs :
        - name : Original
          run  :
        - name : Filtered
          run  : 
      - name : Vector
        subs :
        - name : Zebra
          run  :
        - name : Lane
          run  : 
  - name : Simple Mobility
    subs :
      - name : All
        run  : rosrun sample_data sample_mobility

buttons:
  rosbag_play :
    run : rosbag play
  pmap :
    run :
      selobj : checkbox_point_map_update
      False  : rosrun sample_data sample_points_map
      True   : echo point map update
  vmap :
    run : rosrun sample_data sample_vector_map __args__ swap_x_y_on
  trajectory :
    run : rosrun sample_data sample_trajectory __args__ swap_x_y_off
