repos:
  - repo: https://github.com/ansible/ansible-lint.git
    rev: v25.8.2
    hooks:
      - id: ansible-lint
        args:
          - ansible/
        additional_dependencies:
          - ansible
