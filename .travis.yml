language: generic
services:
  - docker
cache:
  directories:
    - $HOME/.ccache  # can be any valid cache location
env:
  matrix:
    - ROS_DISTRO="kinetic" NOT_TEST_BUILD=true NOT_TEST_INSTALL=true _GUARD_INTERVAL=10 CATKIN_PARALLEL_JOBS=2 CATKIN_PARALLEL_TEST_JOBS=2
#    - ROS_DISTRO="melodic" NOT_TEST_BUILD=true NOT_TEST_INSTALL=true _GUARD_INTERVAL=10
#matrix:
#  allow_failures:
#    - env: ROS_DISTRO="melodic"

install:
  - git clone --quiet --depth 1 https://github.com/ros-industrial/industrial_ci.git .industrial_ci
script:
  - .industrial_ci/travis.sh